'use strict';

var React = require('react/addons');

require('styles/LogoImage.css');

var LogoImage = React.createClass({
    render: function () {
        return (
            <img className="b-logo-image" src="" alt="" />
        );
    }
});

module.exports = LogoImage;

